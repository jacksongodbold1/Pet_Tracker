# Pet_Tracker
Built a GPS tracking device for my dog. It uses LoRa modules to relay GPS coordinates obtained via a built in GPS antenna to a receiver which displays the coordinates on an LCD. Signal integrity and reliability were the main challenges with this project. 
